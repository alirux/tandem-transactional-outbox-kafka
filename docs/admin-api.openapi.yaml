openapi: 3.0.3
info:
  title: Tandem Admin API
  version: 1.0.0
  description: >
    Operational REST API for the Tandem transactional-outbox library. Exposes read access
    to outbox state, plus operational actions (replay, discard, relay pause/resume) useful
    to operators.


    This contract is the source of truth (API-first): the implementation must conform to
    it, and any API change starts here. The API is optional and off by default; when
    enabled it MUST be secured by the host application - Tandem ships the endpoints, not the
    authentication.


    Compatibility. Within a major version (/v1) the contract evolves ADDITIVELY for
    BACKWARD compatibility: only new optional fields, endpoints, or operations are added;
    fields are never removed, renamed, retyped, or made required, value ranges are never
    narrowed, and error `type` slugs never change. Breaking changes ship under a new major
    version (/v2) alongside /v1. For FORWARD compatibility, consumers MUST be tolerant
    readers: ignore unknown response fields and unknown enum values, and never reject a
    payload for containing extra properties. Response schemas are intentionally open (no
    `additionalProperties: false`); do NOT configure strict validation that rejects unknown
    fields - it breaks consumers as soon as the provider adds a field.
  license:
    name: Apache 2.0
    url: https://www.apache.org/licenses/LICENSE-2.0

servers:
  - url: '{basePath}/v1'
    description: >
      Configurable base path + fixed major-version segment. Default effective URL:
      /tandem/admin/v1. A breaking change ships under /v2 alongside /v1.
    variables:
      basePath:
        default: /tandem/admin

security:
  - bearerAuth: []
  - apiKeyAuth: []

tags:
  - name: Outbox
    description: Inspect and act on outbox messages (the sends)
  - name: Relay
    description: Inspect and control the relay

paths:
  /outbox/summary:
    get:
      tags:
        - Outbox
      summary: Outbox health summary
      description: Counts per status plus overall lag metrics. Per-bucket lag is at GET /relay/buckets.
      operationId: getOutboxSummary
      responses:
        '200':
          description: Summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OutboxSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /outbox/messages:
    get:
      tags:
        - Outbox
      summary: Search outbox messages
      operationId: searchOutboxMessages
      parameters:
        - name: status
          in: query
          schema:
            $ref: '#/components/schemas/OutboxStatus'
        - name: aggregateId
          in: query
          schema:
            type: string
        - name: aggregateType
          in: query
          schema:
            type: string
        - name: type
          in: query
          description: CloudEvents event type, e.g. com.acme.order.placed
          schema:
            type: string
        - name: createdFrom
          in: query
          schema:
            type: string
            format: date-time
        - name: createdTo
          in: query
          schema:
            type: string
            format: date-time
        - name: correlationId
          in: query
          description: >
            Exact match on the message's correlation id - the incident-time lookup, since the
            correlation id usually originates outside this application (an inbound request header,
            a consumed message) and is often the only identifier available when an investigation
            starts. One correlation id normally matches MANY messages, across several aggregates,
            so combine it with status to narrow the page.
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
            minimum: 1
            maximum: 500
        - name: cursor
          in: query
          description: Opaque cursor from a previous page (afterId)
          schema:
            type: string
      responses:
        '200':
          description: Page of messages (payload omitted in list view)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OutboxEntryPage'
        '400':
          $ref: '#/components/responses/InvalidParameter'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /outbox/messages/{id}:
    get:
      tags:
        - Outbox
      summary: Get a single outbox message (full detail incl. payload and headers)
      operationId: getOutboxMessage
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Message
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OutboxEntry'
        '400':
          $ref: '#/components/responses/InvalidParameter'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '404':
          $ref: '#/components/responses/NotFound'

  /outbox/messages/{id}/replay:
    post:
      tags:
        - Outbox
      summary: Replay a single message
      description: Resets a DONE or FAILED message to PENDING so the relay re-publishes it.
      operationId: replayMessage
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Replayed; returns the updated message
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OutboxEntry'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Message is not in a replayable state (must be DONE or FAILED)
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
              example:
                type: https://tandem.codingful.com/problems/message-not-replayable
                title: Message is not in a replayable state
                status: 409

  /outbox/messages/{id}/discard:
    post:
      tags:
        - Outbox
      summary: Discard a message (abandon delivery)
      description: >
        Marks a FAILED message as permanently discarded so the relay stops blocking the
        aggregate. WARNING: discarding skips an event and therefore breaks per-aggregate
        happens-before ordering for that aggregate - acknowledgeOrderingBreak must be true.
      operationId: discardMessage
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DiscardRequest'
      responses:
        '200':
          description: Discarded; returns the updated message
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OutboxEntry'
        '400':
          description: acknowledgeOrderingBreak not set
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
              example:
                type: https://tandem.codingful.com/problems/ordering-break-not-acknowledged
                title: Ordering-break acknowledgement is required to discard
                status: 400
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Message is not in a discardable state (must be FAILED)
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
              example:
                type: https://tandem.codingful.com/problems/message-not-discardable
                title: Message is not in a discardable state
                status: 409

  /outbox/replay:
    post:
      tags:
        - Outbox
      summary: Bulk replay by criteria
      description: >
        Replays all DONE/FAILED messages matching the criteria. Supports dryRun to preview
        the matched count without changing anything. At least one selector is required.
      operationId: replayBulk
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReplayRequest'
      responses:
        '200':
          description: Replay result (or dry-run preview)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReplayResult'
        '400':
          description: No selector provided
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
              example:
                type: https://tandem.codingful.com/problems/replay-no-selector
                title: At least one replay selector is required
                status: 400
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /relay/status:
    get:
      tags:
        - Relay
      summary: Relay running state, bucket coverage, and worker count
      operationId: getRelayStatus
      responses:
        '200':
          description: Relay status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RelayStatus'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /relay/pause:
    post:
      tags:
        - Relay
      summary: Pause the relay (optionally a single bucket)
      description: >
        Each relay instance re-reads the desired state on its own control-refresh tick
        (the relay's reclaimInterval, 5s by default), so the effect lags by up to that
        interval - it is not immediate, and a worker mid-dispatch finishes what it already
        claimed. When a bucket is paused, its owning worker keeps renewing the bucket's
        lease and simply stops dispatching sends for it; pause never releases the lease
        (use POST /relay/buckets/{bucket}/release for that). Pausing a single bucket
        requires LEASE coordination and returns 409 otherwise - see the bucket field on
        BucketSelector.
      operationId: pauseRelay
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BucketSelector'
      responses:
        '200':
          description: Updated relay status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RelayStatus'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/CoordinationModeUnsupported'

  /relay/resume:
    post:
      tags:
        - Relay
      summary: Resume the relay (optionally a single bucket)
      description: >
        Each relay instance re-reads the desired state on its own control-refresh tick
        (the relay's reclaimInterval, 5s by default), so the effect lags by up to that
        interval. Resuming a single bucket requires LEASE coordination and returns 409
        otherwise - see the bucket field on BucketSelector.
      operationId: resumeRelay
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BucketSelector'
      responses:
        '200':
          description: Updated relay status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RelayStatus'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/CoordinationModeUnsupported'

  /relay/buckets:
    get:
      tags:
        - Relay
      summary: Per-bucket ownership and lag
      description: >
        Owner, lease, and lag for each virtual bucket. Use to spot uncovered (stalled)
        buckets and hot buckets. LEASE coordination only (standalone or
        embedded-multi-replica): SINGLE keeps bucket ownership in-process and never writes
        tandem_bucket_lease, so there is no per-bucket ownership to report and the request
        returns 409 rather than a misleading reading.
      operationId: getRelayBuckets
      parameters:
        - name: uncoveredOnly
          in: query
          description: Return only buckets that have PENDING rows but no live owner
          schema:
            type: boolean
            default: false
      responses:
        '200':
          description: Bucket statuses
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/BucketStatus'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '409':
          $ref: '#/components/responses/CoordinationModeUnsupported'

  /relay/buckets/{bucket}:
    get:
      tags:
        - Relay
      summary: A single bucket's ownership and lag
      description: >
        Owner, lease, and lag for one virtual bucket. LEASE coordination only, same reasoning
        as GET /relay/buckets - SINGLE returns 409 rather than a misleading reading.
      operationId: getRelayBucket
      parameters:
        - name: bucket
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Bucket status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BucketStatus'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/CoordinationModeUnsupported'

  /relay/buckets/{bucket}/release:
    post:
      tags:
        - Relay
      summary: Force-release a bucket for reassignment
      description: >
        Clears the bucket's ownership lease so another worker can claim it. Use when an owner
        is a zombie (stale heartbeat) and the bucket is stalled. LEASE coordination only -
        under SINGLE there is no bucket ownership to release and this returns 409.
      operationId: releaseBucket
      parameters:
        - name: bucket
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Updated bucket status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BucketStatus'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/CoordinationModeUnsupported'

  /relay/workers:
    get:
      tags:
        - Relay
      summary: Active relay workers
      description: >
        Live relay instances, read from tandem_relay_member. LEASE coordination only, which
        is what registers and renews that presence row; under SINGLE nothing writes it, so
        an empty list would read as "no relay alive" when the relay is perfectly healthy -
        the request returns 409 instead. Use GET /relay/status for a reading that works in
        both modes.
      operationId: getRelayWorkers
      responses:
        '200':
          description: Active workers
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/WorkerInfo'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '409':
          $ref: '#/components/responses/CoordinationModeUnsupported'

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
    apiKeyAuth:
      type: apiKey
      in: header
      name: X-API-Key

  responses:
    InvalidParameter:
      description: A query or path parameter could not be parsed (e.g. an unknown status name, a
        non-numeric id or cursor)
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'
          example:
            type: https://tandem.codingful.com/problems/invalid-parameter
            title: Invalid request parameter
            status: 400
    CoordinationModeUnsupported:
      description: >
        The operation needs per-bucket coordination state, which only LEASE coordination
        maintains, and the relay this database serves runs under SINGLE. Not a failure and
        not a transient condition - the operation is inapplicable to this deployment's
        static configuration (tandem.relay.coordination). The detail names the mode that
        was detected.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'
          example:
            type: https://tandem.codingful.com/problems/relay-coordination-unsupported
            title: Not available under this relay coordination mode
            status: 409
            detail: 'Per-bucket relay control requires LEASE coordination; this relay runs SINGLE'
    Unauthorized:
      description: Missing or invalid credentials
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'
          example:
            type: https://tandem.codingful.com/problems/unauthorized
            title: Missing or invalid credentials
            status: 401
    NotFound:
      description: Resource not found
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'
          example:
            type: https://tandem.codingful.com/problems/not-found
            title: Resource not found
            status: 404
    InternalServerError:
      description: Unexpected internal error
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'
          example:
            type: https://tandem.codingful.com/problems/internal-error
            title: Unexpected internal error
            status: 500

  schemas:
    OutboxStatus:
      type: string
      enum:
        - PENDING
        - IN_FLIGHT
        - DONE
        - FAILED
        - DISCARDED

    OutboxEntry:
      type: object
      description: >
        One entry in the outbox: the message together with its delivery state. This is the
        API's own representation and is deliberately NOT the library's internal model - do
        not assume the two evolve together.
      required:
        - id
        - aggregateId
        - aggregateType
        - status
        - attempts
        - createdAt
      properties:
        id:
          type: integer
          format: int64
        aggregateId:
          type: string
        aggregateType:
          type: string
        type:
          type: string
          nullable: true
          description: CloudEvents event type, e.g. com.acme.order.placed (nullable; HLD §5.1)
        seq:
          type: integer
          format: int64
          nullable: true
          description: >
            The aggregate's sequence number, absent when the message was written without one.
            A producer chooses per message whether to supply a number, let Tandem assign one, or
            store none at all; only the first two put a value here. Absent is not zero - a client
            must not substitute one, because a plausible number is worse than a missing field.
        status:
          $ref: '#/components/schemas/OutboxStatus'
        attempts:
          type: integer
        replays:
          type: integer
          description: >
            How many times an operator has replayed this message, 0 for one never replayed. Unlike
            attempts, which is the delivery budget of the current round and is reset by a replay,
            this counts for the lifetime of the row - it is the audit trail of a replay on the row
            itself, rather than only in the admin log. Deliberately NOT required: an admin instance
            older than this field simply omits it, which a mixed-version rollout must tolerate.
        lastError:
          type: string
          nullable: true
        discardReason:
          type: string
          nullable: true
          description: Operator-supplied reason recorded when this message was discarded (DISCARDED only)
        nextAttemptAt:
          type: string
          format: date-time
          nullable: true
        lockedBy:
          type: string
          nullable: true
        lockedUntil:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        correlationId:
          type: string
          nullable: true
          description: >
            Correlation id captured when the message was written, from its own indexed column - so it
            is present in the list view too, where headers are not read at all. Null for messages
            written with trace/correlation propagation disabled.
        headers:
          type: object
          additionalProperties:
            type: string
          nullable: true
        payload:
          description: Present only in single-message detail, omitted in list view
          nullable: true
          oneOf:
            - type: object
            - type: string

    OutboxEntryPage:
      type: object
      description: >
        One page of search results plus the cursor for the next one. "Page" means a page of
        results, NOT a page number - this API paginates by cursor only (there is no page or
        offset parameter), so pages are traversed forward and cannot be jumped to.
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/OutboxEntry'
        nextCursor:
          type: string
          nullable: true
          description: Pass as cursor to fetch the next page; null when no more

    OutboxSummary:
      type: object
      required:
        - counts
        - lagCount
        - lagAgeSeconds
      properties:
        counts:
          type: object
          description: Row count per status
          properties:
            PENDING:
              type: integer
              format: int64
            IN_FLIGHT:
              type: integer
              format: int64
            DONE:
              type: integer
              format: int64
            FAILED:
              type: integer
              format: int64
            DISCARDED:
              type: integer
              format: int64
        lagCount:
          type: integer
          format: int64
          description: Number of PENDING rows
        lagAgeSeconds:
          type: number
          description: Age of the oldest PENDING row, in seconds

    ReplayRequest:
      type: object
      description: >
        At least one of aggregateId / aggregateType / id range / statuses must be present - a
        selector-less request would replay the entire outbox and is rejected (400
        replay-no-selector).
      properties:
        aggregateId:
          type: string
        aggregateType:
          type: string
        fromId:
          type: integer
          format: int64
        toId:
          type: integer
          format: int64
        statuses:
          type: array
          description: >
            Which statuses are eligible for replay (only DONE/FAILED are valid). When omitted
            alongside another selector, both DONE and FAILED rows matching that selector are
            replayed.
          items:
            $ref: '#/components/schemas/OutboxStatus'
        dryRun:
          type: boolean
          default: false
          description: When true, returns the matched count without modifying anything

    ReplayResult:
      type: object
      required:
        - matched
        - replayed
        - dryRun
      properties:
        matched:
          type: integer
          format: int64
          description: Rows matching the criteria
        replayed:
          type: integer
          format: int64
          description: Rows reset to PENDING (0 on dryRun)
        dryRun:
          type: boolean

    DiscardRequest:
      type: object
      required:
        - acknowledgeOrderingBreak
      properties:
        acknowledgeOrderingBreak:
          type: boolean
          description: Must be true - confirms the operator accepts the per-aggregate ordering break
        reason:
          type: string
          description: Free-text reason, recorded for audit

    BucketSelector:
      type: object
      properties:
        bucket:
          type: integer
          nullable: true
          description: >
            When omitted, the action applies to the whole relay - the only form available
            under SINGLE coordination. Naming a bucket requires LEASE, since per-bucket
            state lives on the tandem_bucket_lease row that only LEASE maintains and reads;
            under SINGLE the request returns 409 rather than accepting a pause the relay
            would never act on. Under LEASE, a bucket outside [0, bucketCount) returns 404.

    BucketStatus:
      type: object
      required:
        - bucket
        - covered
        - paused
        - pendingCount
      properties:
        bucket:
          type: integer
        owner:
          type: string
          nullable: true
          description: Worker id holding the bucket lease; null when unowned
        leaseUntil:
          type: string
          format: date-time
          nullable: true
        covered:
          type: boolean
          description: Whether the bucket has a live owner
        paused:
          type: boolean
          description: >
            Whether this bucket is deliberately paused (via POST /relay/pause with a bucket
            selector). The owning worker keeps renewing its lease while paused - covered can
            be true even when paused is true. Distinguishes an intentionally-idle bucket from
            a genuinely stalled one when pendingCount/lagAgeSeconds is growing.
        pendingCount:
          type: integer
          format: int64
        lagAgeSeconds:
          type: number
          nullable: true
          description: Age of the oldest PENDING row in this bucket, in seconds

    WorkerInfo:
      type: object
      required:
        - workerId
        - bucketCount
      properties:
        workerId:
          type: string
        lastHeartbeat:
          type: string
          format: date-time
        bucketCount:
          type: integer

    RelayStatus:
      type: object
      required:
        - state
        - bucketCount
        - uncoveredBuckets
        - workers
      properties:
        state:
          type: string
          enum:
            - RUNNING
            - PAUSED
            - DOWN
          description: >
            DOWN means no relay instance has heartbeated within the last few
            reclaimIntervals (5s by default, so DOWN typically surfaces within ~15s of
            the last instance stopping) - true under both SINGLE and LEASE coordination,
            since every relay instance touches this heartbeat regardless of mode. A
            relay that was PAUSED before going down still reports DOWN, not PAUSED:
            "is anything running at all" takes priority over the desired-state flag.
        bucketCount:
          type: integer
          description: Total virtual buckets B (fixed)
        uncoveredBuckets:
          type: integer
          description: >
            Buckets with PENDING rows but no live owner (coverage stall). Always 0 under
            SINGLE coordination, which owns every bucket in-process and so has no coverage
            to stall - the same reason the relay's own bucket.uncovered gauge reports
            nothing in that mode.
        workers:
          type: integer
          description: >
            Live relay instances, counted from tandem_relay_member. Always 0 under SINGLE
            coordination, which does not register presence there - it does NOT mean no
            relay is running; check state instead, which covers SINGLE too via a
            separate, lighter heartbeat.

    ProblemDetail:
      type: object
      description: >
        RFC 9457 Problem Details. Returned as media type application/problem+json. Extension
        members beyond the standard fields are permitted.
      additionalProperties: true
      properties:
        type:
          type: string
          format: uri-reference
          description: >
            Canonical problem-type URI. Tandem always sets a stable
            https://tandem.codingful.com/problems/{slug} URL, never about:blank. The {slug}
            (kebab-case) is the stable machine identifier consumers match on; it never changes
            once published. Each URL resolves to a human-readable page documenting the problem
            type.
        title:
          type: string
          description: Short, human-readable summary of the problem type
        status:
          type: integer
          description: HTTP status code
        detail:
          type: string
          description: Human-readable explanation specific to this occurrence
        instance:
          type: string
          format: uri-reference
          description: URI reference identifying the specific occurrence
