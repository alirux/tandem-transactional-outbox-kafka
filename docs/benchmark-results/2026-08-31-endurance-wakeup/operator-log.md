# S9 wakeup endurance run, operator log

All times are local (Europe/Rome), host = the Mac.

| Time | Event |
|---|---|
| 22:11:00 | Run launched: `--duration=21600 --window=1200 --rate=400 --connections=52 --retention=600 --cleanup-interval=30 --cleanup-batch=20000 --wakeup=pg-notify S9`. Rate basis reported as "rate given explicitly". |
| 22:11:0x | Host sampler started (60 s interval), claim sampler started (300 s interval). |
| 22:12:05 | Both relay instances claimed 128 buckets each, coverage 256/256. |
| 22:12:31 | **EXPLAIN #1 (COSTS OFF)**, table filling: 10 496 rows, 21 MB, 7 274 dead. Plan uses `idx_tandem_outbox_dispatch`. Read-only, no locks taken. |
| 22:32:35 | Window 1 reported. 395.2/s, p50=21 ms, p99=211 ms, coverage 256/256. No SEVERE/WARNING in the first 22 minutes. |
| 22:35:07 | **EXPLAIN #2 opened**, table at plateau: 250 188 rows, 446 MB, 44 727 dead, last autovacuum 20:34:33 UTC. |
| 22:35:08 | **#2a `EXPLAIN (COSTS OFF)`** (read-only) and **#2b `EXPLAIN (ANALYZE, BUFFERS)`** (executed) ran back to back, both inside this one second. #2b is the only statement of the whole run that took row locks outside the relay: `LockRows` returned 0 rows, i.e. its single candidate was already held by a concurrent claim and was skipped via `SKIP LOCKED`. It also advanced `idx_scan` on `idx_tandem_outbox_aggregate` by a handful. **The 5-minute claim sample ending 22:36:51 is the one perturbed by this; no other sample is.** |
| 22:35:42 | Index definitions, sizes and status distribution captured (read-only, catalog + one count). |
| 22:52:35 | Window 2 reported: 400.0/s, p50=16 ms, p99=51 ms, pending=4. |
| ~22:33 | **Finding for backlog item 9b, from the claim sampler, independent of the EXPLAINs.** `idx_tandem_outbox_dispatch.idx_scan` stops advancing: 3 829 639 (22:31) -> 4 002 642 (22:41) -> 4 002 721 (22:46) -> 4 002 793 (22:51), i.e. +79 and +72 per 300 s (~0.25/s) against ~1 850/s on `idx_tandem_outbox_aggregate` over the same intervals. The running relay, not just a fresh `psql` plan, has abandoned the dispatch index; the switch coincides with the outbox reaching plateau. The index is still maintained on every insert and every status transition and is now scanned essentially never. |
| 23:12:36 | Window 3 reported: 399.8/s, p50=14 ms, p99=45 ms. |
| 23:15 | **Correction to the 22:52 finding, and the reason the third sampler exists.** The dispatch index is not abandoned, its use *oscillates*: 4 745/s (22:31) -> 0/s for ~15 min (22:46-22:56) -> 5 044/s (23:01) -> 9 402/s (23:11), while `xact_commit` stays flat at ~3 650/s throughout. The two samples read at window 2 fell inside the zero stretch. Latency is indifferent to which plan runs (window 2, mostly aggregate: p50 16 / p99 51; window 3, mostly dispatch: p50 14 / p99 45). Autovacuum reclaiming the dispatch index's bloat is the leading hypothesis, so `sample-vacuum.sh` (60 s, catalog reads only, written for this run) was started at 23:15 to record vacuum counts and both index sizes alongside. |
| 00:53 | **Note for `sample-claims.sh`, from the plateau regimes.** The plan alternation is binary, not gradual: `idx_tandem_outbox_dispatch` runs at ~9 700/s or at 0/s, never in between, flipping every 5-10 min, with `idx_tandem_outbox_aggregate` complementary (1 250/s vs 1 850/s). `xact_commit` is flat at ~3 700/s in both regimes, so the real work is unchanged and only the *counting* differs: the dispatch plan is a `ScalarArrayOpExpr` scan counting 16 per claim (one per owned bucket), the aggregate plan counts 1. **Claims/s derived from `idx_scan` are therefore comparable only at equal plan, and the script should record the active plan next to the counter.** This does not affect the committed poll-vs-wakeup claim measurement, which was taken on a cold outbox where both arms ran the dispatch plan. |
| 01:12:37 | Window 9 reported: 399.9/s, p50=13 ms, p99=43 ms. Half the run done, still zero SEVERE/WARNING. |
| 02:00:57 | **EXPLAIN #3 opened.** Regime probe first: dispatch `idx_scan` 48 604 345 -> 48 604 354 over 30 s = 0/s, i.e. this capture samples the *aggregate* regime (the dispatch regime was the target; capture #4 was armed to wait for it). Table 246 781 rows, 452 MB, 71 109 dead. |
| 02:01:28 | **#3a `EXPLAIN (COSTS OFF)`** and **#3b `EXPLAIN (ANALYZE, BUFFERS)`**, back to back. The plan degraded further than #2: the inner scan's `Index Cond` is gone and the join condition moved up to a `Join Filter` on the nested loop, so both sides now scan the partial index whole and filter. **The anti join is a filtered cross product, quadratic in the pending count.** Cheap only because pending is ~6: execution 0.880 ms (vs 1.790 ms at #2), 104 buffers, planning 13.553 ms. #3b took row locks the same way #2b did; `LockRows` returned 0 rows. **The 5-minute claim sample ending ~02:06 is perturbed by it.** |
| 02:08:52 | **EXPLAIN #4, and the negative result that reframes all four.** The probe caught the dispatch regime for real: dispatch `idx_scan` 48 604 444 -> 48 657 622 over 30 s = **1 772/s**. In that same moment the fresh `psql` plan for the identical query text still chose `idx_tandem_outbox_aggregate`. **So the plan `psql` returns is not the plan the relay executes.** Most likely cause: the relay's prepared statement takes the bucket array as a parameter and gets a generic plan, while these captures hardcode the literal array and hand the planner exact selectivity. Consequence: **captures #1-#4 describe a fresh literal-array plan, not the relay's**, and nothing about the relay's plan may be inferred from them, including the quadratic `Join Filter` shape seen at #3 (that shape is real, but it is psql's plan). Execution 1.587 ms, 104 -> 55 buffers, planning 13.099 ms. #4b took row locks as before; `LockRows` returned 0 rows; **the 5-minute claim sample ending ~02:11 is perturbed by it**. |
| 02:10 | **What item 9b can take from this run**, restricted to what measures the relay itself: (a) the relay's plan choice oscillates binary, ~9 700 dispatch scans/s or 0/s, flipping every 5-10 min; (b) `xact_commit` is flat at ~3 700/s across both regimes, so the real work does not change; (c) throughput and latency are indifferent to the active regime. **Open, and needing a different instrument:** the relay's actual parameterized plan, which requires `auto_explain` server-side or `EXPLAIN EXECUTE` on a prepared statement mirroring the relay's. Not doable on a live run. |
